apiVersion: v1
kind: ConfigMap
metadata:
  name: session-and-task-service-config
  namespace: studyzed
data:
  DEBUG: "True"

  ALLOWED_HOSTS: "*"

  CORS_ALLOWED_ORIGINS_1: http://localhost:5173
  CORS_ALLOWED_ORIGINS_2: http://127.0.0.1:5173
  
  # Database settings
  DB_HOST: "session_task_db"
  DB_PORT: "5432"
