apiVersion: apps/v1
kind: Deployment
metadata:
  name: session-and-task-service-deployment
  namespace: studyzed
spec:
  replicas: 1
  selector:
    matchLabels:    
      app: session-and-task-service
  template:
    metadata:
      labels:
        app: session-and-task-service
    spec:
      containers:
      - name: session-and-task-service
        image: session_and_task_management:latest
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 8000
        envFrom:  
        - configMapRef:
            name: session-and-task-service-config
        - secretRef:
            name: session-and-task-service-secrets
        volumeMounts:
        - name: session-and-task-service-code
          mountPath: /app
        resources:
          requests:
            cpu: "100m"
            memory: "128Mi"
          limits:
            cpu: "250m"
            memory: "256Mi"
        livenessProbe:
          httpGet:
            path: /health/
            port: 8000
          initialDelaySeconds: 10
          periodSeconds: 5
        readinessProbe:
          httpGet:
            path: /health/
            port: 8000
          initialDelaySeconds: 5
          periodSeconds: 5
      volumes:
      - name: session-and-task-service-storage
        # hostPath:
        #   path: "/home/mrlionbyte/Project/Study-Zed/BackendServices/Usermanagement_Service/Usermanagement"
        #   type: Directory
        persistentVolumeClaim:
          claimName: session-and-task-pvc

---
apiVersion: v1
kind: Service
metadata:
  name: session-and-task-service
  namespace: studyzed
spec:
  selector:
    app: session-and-task-service
  ports:
    - port: 8009
      targetPort: 8000
  type: ClusterIP